enum Operation {
    QUERY,
    MUTATION,
    SUBSCRIPTION
}

input PermissionInput {
    operation: Operation!
    query: String! @NotBlank
}

type Permission {
    operation: Operation!
    query: String!
}
