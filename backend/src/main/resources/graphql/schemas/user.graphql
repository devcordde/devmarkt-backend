union UserResponse = User | Failure

input UserIdInput {
    type: String! @NotBlank
    id: Long
}

type UserId {
    type: String!
    id: Long
}

type User {
    userId: UserId!
    roles: [String!]! # TODO: use enum for roles
}
