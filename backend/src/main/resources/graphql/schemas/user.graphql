union UserResponse = UserSuccess | UserFailed

input UserIdInput {
    type: String! @NotBlank
    id: Long
}

type UserId {
    type: String!
    id: Long
}

type UserSuccess {
    userId: UserId!
    roles: [String!]!
}

type UserFailed implements Error {
    errorCode: String!
    message: String!
}
