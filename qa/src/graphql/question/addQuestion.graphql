mutation($templateName: TemplateName!, $input: QuestionAddInput!) {
    addQuestion(templateName: $templateName, question: $input) {
        ... on QuestionSuccess {
            question
            number
            multiline
            minAnswerLength
        }

        ... on QuestionFailed {
            message
            errorCode
        }
    }
}
