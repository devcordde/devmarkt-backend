mutation($userId: UserIdInput!, $roles: [String!]!) {
    addUserRoles(userId: $userId, roles: $roles) {
        ... on UserSuccess {
            userId {
                type
                id
            }
            roles
        }
        ... on UserFailed {
            errorCode
            message
        }
    }
}
