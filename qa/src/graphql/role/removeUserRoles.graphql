mutation($userId: UserIdInput!, $roles: [String!]!) {
    removeUserRoles(userId: $userId, roles: $roles) {
        ... on UserSuccess {
            userId {
                type
                id
            }
            roles
        }
        ... on UserFailed {
            errorCode
            message
        }
    }
}
