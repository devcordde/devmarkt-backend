mutation {
    createTemplate(questions: [{number: 0, question: "Halts fmaul"}], name: "Nudfelauflauf") {
        __typename
        ...on TemplateSuccess {
            questions {
                number
                question
            }
        }
        ...on TemplateFailed {
            errorCode
            message
        }
    }
}
