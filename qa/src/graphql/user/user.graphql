query ($id: UserIdInput!) {
    user(id: $id) {
        ... on User {
            role
            id {
                number
                type
            }
        }
        ... on Failure {
            errorCode
            message
        }
    }
}
